[project]
name = "wwh"
version = "26.2"
description = "Sourcegen-based Distrobase installer."
requires-python = ">=3.12"

dependencies = []

[project.scripts]
h2 = "hydrogen:entrypoint"

[build-system]
requires = ["hatchling<1.32"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hydrogen"]